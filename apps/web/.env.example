# Browser/simulator development.
VITE_API_BASE_URL=http://localhost:18081/api/v1

# Physical device on the local network (replace with the Mac's current IP).
# On a physical device, localhost means the device itself.
# VITE_API_BASE_URL=http://<IP_DEL_MAC>:18081/api/v1

# Production must use a public HTTPS endpoint.
